# 核心依赖（基础功能）
python-dotenv==1.0.0
requests==2.31.0  # 与节点通信
flask==2.3.3      # 提供HTTP接口

# 联邦学习核心
flwr==1.5.0       # 联邦学习框架
tenseal==0.3.16   # 同态加密（隐私保护）

# 数据处理与模型操作
numpy==1.26.0     # 模型参数聚合计算

# 可选：差分隐私增强
opacus==1.4.0     # 若需要更复杂的DP实现（可选）